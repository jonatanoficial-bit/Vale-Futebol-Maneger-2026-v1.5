/* engine/market.js — Mercado (placeholder) */
(function () {
  'use strict';
  const NS = (window.VFM26 = window.VFM26 || {});
  const Market = {
    listShort() { return []; }
  };
  NS.Engine = NS.Engine || {};
  NS.Engine.Market = Market;
})();