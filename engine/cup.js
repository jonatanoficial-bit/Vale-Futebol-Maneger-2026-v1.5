/* engine/cup.js — Copa do Brasil (placeholder) */
(function () {
  'use strict';
  const NS = (window.VFM26 = window.VFM26 || {});
  const Cup = {
    // Fase futura: chaveamento, mandos, fases
    createBracket() {
      return { rounds: [] };
    }
  };
  NS.Engine = NS.Engine || {};
  NS.Engine.Cup = Cup;
})();